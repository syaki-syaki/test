<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Issueのタイトル入力用 EditText -->
    <EditText
        android:id="@+id/issueTitleEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="タイトルを入力してください"
        android:inputType="text"
        android:padding="8dp"
        android:layout_marginBottom="8dp" />

    <!-- Issueの内容入力用 EditText -->
    <EditText
        android:id="@+id/issueBodyEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="内容を入力してください"
        android:inputType="textMultiLine"
        android:minLines="4"
        android:gravity="top"
        android:padding="8dp"
        android:layout_marginTop="8dp" />

    <!-- Issue作成ボタン -->
    <Button
        android:id="@+id/createIssueButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Issueを作成"
        android:layout_marginTop="16dp" />
</LinearLayout>
